const r=require('express').Router(),c=require('../controllers/backlog.controller'),cp=require('../controllers/checkpoint.controller'),s=require('../controllers/session.controller');r.get('/',c.list);r.post('/',c.create);r.get('/:userGameId/checkpoints',cp.list);r.post('/:userGameId/checkpoints',cp.create);r.get('/:userGameId/sessions',s.list);r.post('/:userGameId/sessions/start',s.start);r.get('/:id',c.get);r.put('/:id',c.update);r.delete('/:id',c.remove);module.exports=r;
